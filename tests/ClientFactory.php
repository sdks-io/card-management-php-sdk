<?php

declare(strict_types=1);

/*
 * ShellCardManagementAPIsLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

namespace ShellCardManagementAPIsLib\Tests;

use Core\Types\CallbackCatcher;
use ShellCardManagementAPIsLib\Authentication\ClientCredentialsAuthCredentialsBuilder;
use ShellCardManagementAPIsLib\ShellCardManagementAPIsClient;
use ShellCardManagementAPIsLib\ShellCardManagementAPIsClientBuilder;

class ClientFactory
{
    public static function create(CallbackCatcher $httpCallback): ShellCardManagementAPIsClient
    {
        $clientBuilder = ShellCardManagementAPIsClientBuilder::init();
        $clientBuilder = self::addConfigurationFromEnvironment($clientBuilder);
        $clientBuilder = self::addTestConfiguration($clientBuilder);
        return $clientBuilder->httpCallback($httpCallback)->build();
    }

    public static function addTestConfiguration(
        ShellCardManagementAPIsClientBuilder $builder
    ): ShellCardManagementAPIsClientBuilder {
        return $builder;
    }

    public static function addConfigurationFromEnvironment(
        ShellCardManagementAPIsClientBuilder $builder
    ): ShellCardManagementAPIsClientBuilder {
        $timeout = getenv('SHELL_CARD_MANAGEMENT_AP_IS_LIB_TIMEOUT');
        $numberOfRetries = getenv('SHELL_CARD_MANAGEMENT_AP_IS_LIB_NUMBER_OF_RETRIES');
        $maximumRetryWaitTime = getenv('SHELL_CARD_MANAGEMENT_AP_IS_LIB_MAXIMUM_RETRY_WAIT_TIME');
        $environment = getenv('SHELL_CARD_MANAGEMENT_AP_IS_LIB_ENVIRONMENT');
        $oAuthClientId = getenv('SHELL_CARD_MANAGEMENT_AP_IS_LIB_O_AUTH_CLIENT_ID');
        $oAuthClientSecret = getenv('SHELL_CARD_MANAGEMENT_AP_IS_LIB_O_AUTH_CLIENT_SECRET');

        if (!empty($timeout) && \is_numeric($timeout)) {
            $builder->timeout(intval($timeout));
        }

        if (!empty($numberOfRetries) && \is_numeric($numberOfRetries)) {
            $builder->numberOfRetries(intval($numberOfRetries));
        }

        if (!empty($maximumRetryWaitTime) && \is_numeric($maximumRetryWaitTime)) {
            $builder->maximumRetryWaitTime(intval($maximumRetryWaitTime));
        }

        if (!empty($environment)) {
            $builder->environment($environment);
        }

        if (!empty($oAuthClientId) && !empty($oAuthClientSecret)) {
            $builder->clientCredentialsAuthCredentials(
                ClientCredentialsAuthCredentialsBuilder::init($oAuthClientId, $oAuthClientSecret)
            );
        }

        return $builder;
    }
}
